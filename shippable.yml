language: java
jdk:
- openjdk8

services:
- selenium

script:
- java -jar selenium-server-standalone-2.53.1.jar -role hub
- java -jar selenium-server-standalone-2.53.1.jar -role node -port 5579

build:
ci:
- mvn test
