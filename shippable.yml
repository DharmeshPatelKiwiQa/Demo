language: java
jdk:
- openjdk8

services:
- selenium

build:
ci:
- sudo chmod +X /root/src/github.com/DharmeshPatelKiwiQa/Demo/src/test/resources/chromedriver
- sudo mvn -version
- sudo mvn install
- "export DISPLAY=:99.0"
- xvfb-run --server-args="-ac" mvn test